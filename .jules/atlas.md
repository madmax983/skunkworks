## 2024-05-18 - [Math/Bitwise Operator Untangling]
**Tangle:** The Blob - `experiments/chimera-lang/src/vm/mod.rs` had multiple fundamental core math and bitwise operations (`Mod`, `BitAnd`, `BitOr`, `BitXor`, `BitNot`, `Shl`, `Shr`) improperly placed inside `exec_havoc_op` rather than their correct domain context inside `exec_math_op`.
**Blueprint:** Extracted these mathematical and bitwise execution blocks from `exec_havoc_op` and relocated them to `exec_math_op` within the dedicated `experiments/chimera-lang/src/vm/ops/math.rs` module. This enforces strict domain boundaries, reduces the size of `mod.rs`, and ensures that `exec_havoc_op` is solely responsible for its designated mutation domain (`HavocRate` and `HavocScope`).

## 2026-03-27 - [TUI App Refactoring / Structure Facade Extraction]
**Tangle:** The Blob - `experiments/chimera-lang/src/tui/mod.rs` was massive and contained a large blob of UI state view mappings and rendering code. It essentially duplicated `experiments/chimera-lang/src/tui/app/mod.rs` logic.
**Blueprint:** Cleaned up the `mod.rs` by turning it into a facade, and extracting the monolithic utilities to a new file `experiments/chimera-lang/src/tui/utils.rs`. Re-pointed the `run_tui` loop to delegate entirely to the `app::run_app` module. Resolved compilation issues around returning boolean values that indicate TUI exit/continue inside `chars.rs` and `actions.rs` that broke during previous splitting iterations.
## 2026-03-27 - [TUI App Refactoring / Structure Facade Extraction]
**Tangle:** The Blob - `experiments/chimera-lang/src/tui/mod.rs` was massive and contained a large blob of UI state view mappings and rendering code. It essentially duplicated `experiments/chimera-lang/src/tui/app/mod.rs` logic.
**Blueprint:** Cleaned up the `mod.rs` by turning it into a facade, and extracting the monolithic utilities to a new file `experiments/chimera-lang/src/tui/utils.rs`. Re-pointed the `run_tui` loop to delegate entirely to the `app::run_app` module. Resolved compilation issues around returning boolean values that indicate TUI exit/continue inside `chars.rs` and `actions.rs` that broke during previous splitting iterations.
