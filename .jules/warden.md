**2026-06-25 - [Rand Unsoundness in Sonic-Glacier]**
**Threat:** The `sonic-glacier` experiment depended on an older `noise` crate (v0.8.2), which in turn relied on `rand v0.7.3`. This specific version of `rand` contains an advisory (RUSTSEC-2026-0097) indicating unsound behavior when a custom logger uses `rand::rng()`. While `sonic-glacier` may not have actively used the logger in this manner, leaving an unsound dependency in the workspace exposes the project to subtle race conditions and memory safety vulnerabilities in a multithreaded context or if logging behavior changes.
**Defense:** Updated the `noise` crate dependency from `"0.8"` to `"0.9"` in `experiments/sonic-glacier/Cargo.toml`. This automatically upgrades its transitive `rand` dependency to the secure `v0.8.6`, resolving the RUSTSEC vulnerability across the workspace.

**2026-06-25 - [DoS via Integer Parsing Panics]**
**Threat:** Several components—`experiments/stego-cartridge/src/vm.rs`, `experiments/stego-cartridge/src/asm.rs`, `experiments/pixel-archivist/src/art.rs`, and `experiments/bio-chain/src/types.rs`—used `.unwrap()` when converting arbitrary byte slices (which may be user-controlled or malformed) into integers via `try_into()`. Specifically, `try_into().unwrap()` was used on `&[u8]` arrays to build `i32`, `u32`, and `i64` values. If the byte slice length was incorrect (even due to a logic bug elsewhere), the application would deterministically panic, causing a Denial of Service.
**Defense:** Replaced `try_into().unwrap()` with `.unwrap_or([0; N])` to ensure a safe fallback value, thereby preventing crashes when parsing malformed slices. In `bio-chain`, also replaced `serde_json::to_string().unwrap()` with a safe `if let Ok(...)` fallback, and `duration_since().unwrap()` with `unwrap_or(0)` to prevent panic crashes on system time anomalies.

**2026-06-25 - [Unbounded File Read OOM DoS Prevention IV]**
**Threat:** Several components across the workspace (`mnem-strings`, `chron-compost`, `chimera-lang`, `mnem-mycelium`, `struct-harmonics`, `mnem-fluid`, `syntax-garden`, `hidden-brush`, `chrontext`, `code-bio-dome`, `mnem-rot`, `digital-compost`, `chromatic-code`, `heap-arena`, `ferrous-cladistics`, `ferrous-neuron`, `compost-chimera`, `ferrous-graph`, `cargo-rocket`, `syntax-fugue`, `chimera-hologram`, `semantic-spy`, `mnem-ddos`, `miura-interface`, `stego-cartridge`, `chron-fluid`) implemented an OOM protection pattern using `std::io::Read::read_to_string(&mut std::io::Read::take(file, limit + 1), &mut content)`. However, this was syntactically incorrect and failed to compile under newer Rust versions or when imported incorrectly, creating a maintenance hazard. In other instances, `std::fs::read_to_string` was used with no bounds checks at all. Both scenarios presented a Denial of Service (DoS) vulnerability where processing extremely large files (e.g., symlinks to `/dev/zero` or huge malformed inputs) could exhaust system memory or panic the process.
**Defense:** Replaced `std::io::Read::read_to_string(...)` and `std::fs::read_to_string` with the idiomatic, memory-safe approach: `file.take(limit + 1).read_to_string(&mut content)`. Validated that the total read size does not exceed the imposed 1MB (or 10MB) limit before proceeding with processing. This prevents memory exhaustion while ensuring truncated inputs aren't processed silently.

**2026-06-25 - [Akashic Records Data Loss]**
**Threat:** The `chimera-lang` VM's `AkashicRecords` implementation returned an empty, uncorrupted (`corrupted: false`) database when it encountered malformed JSON data upon load. A subsequent write operation to the same records instance would then overwrite and permanently destroy the corrupted file's existing data, leading to irreversible data loss without user consent.
**Defense:** Fixed the deserialization fallback to return `Err("Read Error".to_string())` instead of a silent fallback to an empty database. This correctly propagates the error during initialization and prevents an accidental overwrite of the corrupted JSON store, ensuring that data is protected and that operations abort safely.

**2026-06-25 - [NaN/Infinity Propagation DoS in Flocking Simulation]**
**Threat:** The `compute_force` calculation in `crates/flocking/src/lib.rs` blindly trusted input positions and velocities. If a single agent's state became corrupted with `f64::NAN` or `f64::INFINITY` (e.g. from division by zero in upstream logic, physics explosions, or malicious data injection), these invalid values would silently propagate during the distance and steering calculations, eventually infecting the entire flock's simulation state.
**Defense:** Added explicit `is_finite()` validation checks at the boundary of `compute_force` and inside the neighbor accumulation hot loop. If non-finite values are detected, they are discarded (neighbors are ignored, and corrupted agents return zero force), safely containing the corruption and preventing cascade failure.

**2026-06-25 - [String Concatenation Memory Exhaustion]**
**Threat:** The `chimera-lang` VM contained a vulnerability in `experiments/chimera-lang/src/vm/ops/math.rs` where the `OpCode::Add` logic executed string concatenation (`s1 + &s2`) without verifying the resulting size. This allowed an attacker to create a "String Bomb" (e.g., repeatedly duplicating and concatenating strings inside a loop) that quickly exhausted system memory, leading to an Out-Of-Memory (OOM) panic and Denial of Service.
**Defense:** Fortified `OpCode::Add` by validating that `s1.len().saturating_add(s2.len())` does not exceed `crate::vm::MAX_STRING_LEN` (65,536 bytes) prior to concatenation. If the limit is breached, the operation aborts safely and pushes an "Error: String length exceeds maximum allowed length" message to the VM output, neutralizing the DoS vector.
2025-02-18 - [poincare-disk Geodesic NaN Poisoning]
**Threat:** Geodesic radius NaN Poisoning due to division by zero
**Defense:** Return None if determinant is NaN in euclidean_circle

**2025-02-18 - [Infinity Propagation DoS via Squared Math Overflow]**
**Threat:** The `Vec2` and `Vec4` geometry primitives in `crates/locus` blindly returned the result of squared length/distance calculations. If coordinates were exceedingly large (e.g. `f32::MAX`), the internal squaring operation (`x*x + y*y...`) overflowed to `Infinity`. This bypassed finite assertions and limits, propagating Infinity into dependent systems and potentially causing a Denial of Service via corrupted logic or subsequent NaN generation.
**Defense:** Explicitly clamped infinite mathematical overflows in `length_squared`, `magnitude_squared`, and `distance_squared` to `std::f32::MAX` (for `Vec4`) and `std::f64::MAX` (for `Vec2`), ensuring bounds are respected and propagation is prevented.

**2025-02-18 - [DoS via Topology Normalize Overflow]**
**Threat:** The `Topology::normalize` method in `crates/locus/src/topology.rs` blindly executed `.div_euclid()` and `.rem_euclid()` on coordinates. In Rust, if the dividend is `i64::MIN` and the divisor evaluates to `-1` or `0`, these operations panic. While the grid dimensions are positively bounded in normal usage, a logic bug, boundary failure, or malicious coordinate input (e.g. integer underflow creating `i64::MIN`) combined with edge-case divisors could trigger an application crash, representing a Denominator DoS vector.
**Defense:** Replaced all `.div_euclid()` and `.rem_euclid()` calls inside the method with `.checked_div_euclid().unwrap_or(0)` and `.checked_rem_euclid().unwrap_or(0)` to ensure any mathematical overflow is safely caught and defaults to a valid coordinate.
**2025-05-24 - [PbdSystem4D NaN DoS Vulnerability]**
**Threat:** Unvalidated parameters (e.g., NaN `min_len`, `max_len`, `stiffness`, or `friction`) in `PbdSystem4D` methods and extreme `vel` values in integration allowed properties to explode to Infinity and propagate to `NaN`. This corrupted the physics state, causing explicit engine panics on constraints solver routines (`NaN detected in particle distance`), representing a Denial of Service (DoS) vulnerability.
**Defense:** Applied Defense in Depth. 1) Validated inputs at the boundary in `add_actuator_constraint` and `add_distance_constraint` ensuring finite properties. 2) Clamped the integration layer by enforcing `friction.is_finite()` and only updating `p.pos` if `new_pos.is_finite()`, resetting explosive velocity to zero to contain numerical corruption.

**2024-05-28 - [Security Audit]**
**Threat:** Potential Denial of Service (DoS) and path traversal via `OpCode::Scavenge` and `OpCode::Digest`.
**Defense:** Confirmed existing bounds checking limits file allocations to 1MB and sanitizes path directories via `canonicalize` and `starts_with` against the sandbox root. Memory boundaries are protected.

**2025-05-18 - [Out-Of-Bounds Read/Write Panic in `neuro-sim`]
**Threat:** The `Network` struct in `neuro-sim/src/lib.rs` exposed its `inputs` and `spikes` vectors publicly. If an attacker or user logic dynamically cleared or truncated these vectors, the `step()` and `update_neurons()` functions would index directly into them (`inputs[i]` and `spikes[i]`) causing an out-of-bounds slice indexing panic leading to Denial of Service (DoS).
**Defense:** Added explicit boundary resizing checks at the beginning of `step()` to ensure `inputs` and `spikes` are appropriately sized relative to `neurons.len()`. Furthermore, replaced direct indexing in `update_neurons()` with safe slice boundary handlers (`inputs.get(i)` and `spikes.get_mut(i)`).
**2026-10-07 - [LRU Unsoundness]**
**Threat:** The `lru` crate version 0.12.5 used by `ratatui` version 0.26.3 violates Stacked Borrows by invalidating the internal pointer during `IterMut` iteration. This allows Undefined Behavior (UB) and memory corruption during iteration.
**Defense:** Updated the `ratatui` crate dependency to version `0.30` across the codebase, which bumps the transitive dependency on `lru` to version `0.16.4`, resolving the memory safety bug (RUSTSEC-2026-0002).
**2025-06-07 - Integer Overflow DoS in Miller Lattice**
**Threat:** The `place_children` coordinate calculations in `crates/miller-lattice/src/lib.rs` were vulnerable to integer overflows (Denial of Service). If an adversary or extensive tree triggered coordinate bounds to exceed `i32::MAX`, it caused an unhandled panic in debug mode and potentially unexpected wrapping behavior in release, leading to system crash or data corruption.
**Defense:** Replaced raw addition and multiplication operators (`+` and `*`) with explicit bounded mathematical operations (`saturating_add` and `saturating_mul`). This safely clamps coordinates at maximum/minimum boundaries, preventing crashes.
**2026-10-08 - [Fabric-Limb Arm Solver Out-Of-Bounds Panic]**
**Threat:** The `Arm::solve` method in `experiments/fabric-limb/src/physics.rs` blindly accessed `self.joints[0]` and used `self.joints.last().unwrap()` without checking if the public `joints` vector was empty. If an attacker or external logic cleared the vector, this caused a deterministic out-of-bounds array access panic, leading to a Denial of Service (DoS).
**Defense:** Added an early return check `if self.joints.is_empty() { return; }` to `Arm::solve`. Replaced all remaining unwrap calls on `self.joints.last()` with safe `.unwrap_or()` fallbacks, ensuring the simulation simply ignores the request or returns zero instead of exploding. Updated the `havoc` integration test to confirm the process now succeeds without crashing.
2026-10-07 - [LRU Unsoundness]
**Threat:** The `lru` crate version 0.12.5 used by `ratatui` version 0.26 violates Stacked Borrows by invalidating the internal pointer during `IterMut` iteration. This allows Undefined Behavior (UB) and memory corruption during iteration.
**Defense:** Updated the `ratatui` crate dependency to version `0.30` across the codebase, which bumps the transitive dependency on `lru` to version `0.18.0`, resolving the memory safety bug (RUSTSEC-2026-0002).
**2024-05-28 - [Hologram Out-Of-Bounds Panic]**
**Threat:** The `Hologram` struct in `experiments/chaos-hologram` and `experiments/hologram-text` exposed its `width`, `height`, and `data` fields publicly. This allowed arbitrary external code to modify the dimensions without resizing the underlying `data` vector. A subsequent call to `.reconstruct()` would then use the invalid dimensions to compute an index, resulting in an out-of-bounds slice panic and leading to a Denial of Service.
**Defense:** Privatized the `width`, `height`, and `data` fields to enforce encapsulation, providing read-only getters for external consumers. Additionally, injected a robust boundary check `assert_eq!(self.width * self.height, self.data.len())` at the start of `.reconstruct()` to ensure state integrity. Migrated tests to inline `#[cfg(test)]` modules to allow for controlled internal fuzzing without compromising the public API.
**2026-10-09 - [OOM and Memory Leak via Cladistics / Singularity Cycle]**
**Threat:** The `Singularity` opcode in `experiments/chimera-lang/src/vm/nova.rs` concatenated all strands' genes without bounds checking and failed to reset the `vm.cladistics` node graph. An attacker could exploit this by looping `Mitosis` and `Singularity` commands, resulting in exponential memory exhaustion through unbounded gene amalgamation and an indefinitely growing `cladistics` graph, causing the VM to OOM and crash the application.
**Defense:** Applied Defense in Depth. 1) Enforced a safe cap (`65536`) on the size of the merged gene array inside `exec_singularity`, truncating excess data to prevent exponential memory blowout. 2) Reinitialized `vm.cladistics = Cladistics::new()` upon Singularity execution, successfully pruning the stale node graph and neutralizing the memory leak. Verified fix by enabling the `test_cladistics_memory_leak` integration test.

**2026-10-09 - [IPC File Stealing via Incomplete Extension Check]**
**Threat:** The inter-process communication logic in `experiments/chimera-lang/src/vm/ipc.rs` attempted to filter out locked files during the `receive` scan using `path.extension().is_some_and(|ext| ext == "json")`. However, when checking a lock file created via `path.with_extension("json.lock")` (e.g., `msg.json.lock`), the `Path::extension()` returns `"lock"`, not `"json"`. Therefore, the lock file effectively bypassed the condition or prevented other valid JSONs from being seen properly if the matching was flawed. In addition, the renaming logic caused a race condition where the recipient could steal another process's lock file.
**Defense:** Tightened the validation boundary in `ipc.rs` by adding a suffix guard `!path.to_string_lossy().ends_with(".lock")` alongside the existing extension check, ensuring lock files are unequivocally ignored during message polling and effectively neutralizing the IPC lock-stealing vector.

**2026-10-10 - [Recursive Trait DoS via Deeply Nested Structures]**
**Threat:** The `Cord` struct in `crates/quipu` allowed unbounded hierarchical nesting via its `subsidiaries` vector. The structure implicitly relied on auto-derived implementations for `Clone` and `Debug` (`#[derive(Clone, Debug)]`). Since these auto-derived traits map directly to recursive function calls, an attacker could construct a deeply nested `Cord` structure (e.g. 100,000 layers deep) and subsequently trigger a fatal stack overflow panic upon cloning or debug printing the root node, resulting in a Denial of Service (DoS) vulnerability.
**Defense:** Replaced the derived `Clone` and `Debug` implementations with secure, manual implementations. For `Clone`, implemented an iterative post-order traversal using a heap-allocated vector `stack` to securely reconstruct the tree without consuming the call stack. For `Debug`, implemented a recursive call with a safe depth limit (100 levels) via a `cap_depth` helper, gracefully truncating the output at the boundary limit. Updated integration havoc assertions to verify that execution survives deep tree operations safely.

**2026-10-10 - [AST Parsing Stack Overflow DoS]**
**Threat:** The `generate_level` function in `experiments/heap-arena/src/level_gen.rs` used `syn::parse_file` to parse arbitrary Rust source code. Because `syn` uses a recursive descent parser, an attacker could supply a deeply nested file (e.g. thousands of nested `{` or `(`) to cause a deterministic stack overflow and crash the application, resulting in a Denial of Service (DoS).
**Defense:** Implemented an O(N) linear scan over the file contents before parsing to explicitly calculate the maximum nesting depth of `{`, `(`, and `[`. If the nesting depth exceeds a safe limit (128), the file is skipped, preventing the stack overflow DoS. Verified the mitigation by ensuring the Havoc test suite gracefully handles deeply nested files without panicking.

**2024-05-19 - [Parser and Evolution Unsoundness]**
**Threat:** The `chimera-lang` VM contained `unwrap()` calls on iterators immediately following AST parsing in `oracle.rs`, `nova_quantum.rs`, `nova_paleontology.rs`, and `nova_genetics.rs`. If the parser matched but yielded an empty iterator, this would cause a deterministic panic (DoS). Additionally, `evolution.rs` contained an `unwrap()` and an unbounded range selection on `pool`, which could panic if the pool was empty. Finally, `ipc.rs` contained an `unwrap()` on `SystemTime::now().duration_since(UNIX_EPOCH)`, which could panic if the system time was set before the Unix epoch (clock drift or malicious time setting).
**Defense:** Replaced all vulnerable `unwrap()` calls with safe fallbacks (`?`, `unwrap_or_default()`, or early returns).

**2026-04-18 - [DoS via Unsafe Unwraps in Git DAG Traversal]**
**Threat:** The `git-rogue` experiment used `.unwrap()` and `.expect()` when traversing Git commit DAG nodes and casting user inputs (`to_digit(10).unwrap()`). This assumes the Git repository structure and inputs are perfectly aligned with internal logic, exposing the system to a panic/DoS if the repository state drifts, inputs are unhandled, or DAG structure is missing.
**Defense:** Replaced panicking accessors with safe `Option` checking (`if let Some(digit) = ...`, `if let Some(node) = ...`) in `main.rs`, `game.rs`, and `ui.rs`, returning graceful defaults or errors instead of crashing the TUI application.

**2026-07-26 - [quick-xml Namespace DoS]**
**Threat:** The `quick-xml` crate versions before 0.41.0 were vulnerable to memory-exhaustion denial of service (DoS) due to unbounded namespace-declaration allocations and quadratic run time (RUSTSEC-2026-0194, RUSTSEC-2026-0195).
**Defense:** Upgraded `quick-xml` to `0.41.0` by bumping `wayland-scanner` to version `0.31.11` via `cargo update -p wayland-scanner`.

**2026-10-10 - [Chimera-Tardis OpenGL Scissor UB]**
**Threat:** The `with_scissor` function in `experiments/chimera-tardis/src/safe_gl.rs` failed to clamp the results of `intersect_rect` before passing them to the unsafe `glScissor` function. Since `intersect_rect` could return values up to `i32::MAX`, this allowed out-of-bounds coordinates to overflow the driver's internal 16-bit integers, causing Undefined Behavior and a Denial of Service (DoS) panic.
**Defense:** Explicitly applied mathematical boundary clamping (`x.clamp(-16384, 16384)` and `w.clamp(0, 32768)`) to the final intersection coordinates *before* executing the unsafe `glScissor` block, neutralizing the integer overflow threat. Added a `havoc_tests` module to prove safety under fuzzed inputs.
