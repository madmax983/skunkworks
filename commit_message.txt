🛡️ Sentry: [test coverage improvement]

🎯 Target: `crates/gray-scott/src/lib.rs` (GrayScott::update_parallel)
💣 Risk: `rayon::iter::par_chunks_exact_mut` will panic if the `chunk_size` provided to it is 0. This happens when the dimensions of a simulation (like Gray-Scott) are initialized to 0 and the simulation step is called using the `parallel` feature.
🧪 Strategy: Added early return when `w == 0 || h == 0` in `update_parallel`, similar to the sequential version. The existing `test_zero_dimensions` unit test failed with the parallel feature, which is now fixed.
🔬 Verification: `cargo test -p gray-scott --features parallel`
