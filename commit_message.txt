🗺️ Atlas: [Extraction of exec_nova_dispatch from mod.rs]

🕸️ Tangle: The structural problem was that `experiments/chimera-lang/src/vm/mod.rs` was heavily bloated, largely due to `exec_nova_dispatch` being a monolithic 400+ line `match` block tightly coupling Nova logic into the main VM definition.
📐 Blueprint: Extracted `exec_nova_dispatch` from `mod.rs` into a new `experiments/chimera-lang/src/vm/ops/nova_dispatch.rs` module under the existing `impl crate::vm::ChimeraVM` block, and imported it via `vm/ops/mod.rs`.
🧱 Stability: Reduced coupling between core VM execution and Nova expansion, significantly improving modularity and reducing the line count of the core `mod.rs` file.
🔬 Verification: Builds successfully, strict separation enforced. Tested via `cargo check` and `cargo test -p chimera-lang` to verify zero regressions.
