📜 Codex: ADR 086 & Architecture Diagram Update

🧠 **Decision:** Recorded the move to eliminate the massive 4,500+ line run_app event loop duplication in `tui/mod.rs` and correctly delegate to `tui/app/mod.rs` in `chimera-lang`. Addressed previous feedback by moving status to Accepted, fixing diagram syntax, and updating sequence diagram.
🗺️ **Visuals:** Updated the architecture diagram in `docs/architecture.md` by linking the Chimera TUI Architecture section to ADR 086. The existing Mermaid diagram `TuiFacade --> AppLoop : Delegates event loop` perfectly captures the new boundaries. Added the updated sequence diagram indicating how the Facade calls `run_app`.
🔗 **Link:** See `docs/adr/086-chimera-tui-blob-elimination.md`.
