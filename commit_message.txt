Razor: Remove 1-variant enums

Following Razor's philosophy, 1-variant enums were flattened to reduce abstraction:
- Flattened `AudioCommand` enum into a single struct across `genetic-luthier`, `git-strings`, `chimera-strings`, `ferrous-strings`, `string-theory`, `neuro-strings`, `locus-strings`, `gray-strings`.
- Flattened `VisualEffect` and `MidiEvent` into structs in `chimera-lang/src/vm/mod.rs`.
- Fixed several Clippy warnings regarding dead code and initialization across multiple workspace projects (`chaos-pendulum`, `syntax-garden`, `lattice-brain`, `neuro-circuit`, `mnem-hologram`, `chaos-hologram`).
