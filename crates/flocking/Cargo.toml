[package]
name = "flocking"
version = "0.1.0"
edition = "2021"
description = "Craig Reynolds' Boids algorithm"

[dependencies]
locus = { path = "../locus" }
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
[dev-dependencies]
proptest = "1.0"
