[package]
name = "platter"
version = "0.1.0"
edition = "2021"

[dependencies]
# Fixes "multiple glam packages" ambiguity by explicitly depending on the version used by bevy_reflect (via other crates)
glam = "0.27.0"
