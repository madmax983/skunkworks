#[cfg(test)]
mod tests {
    use crate::vm::ChimeraVM;

    // Looks like loom is not a dependency in this crate directly.
    // Let's verify by adding loom to dev-dependencies and running a basic test.
}
