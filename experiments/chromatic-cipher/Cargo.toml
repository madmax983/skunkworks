[package]
name = "chromatic-cipher"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
image = "0.25"
rand = { workspace = true }
anyhow = { workspace = true }
mlua = { version = "0.9", features = ["lua54", "vendored", "macros"] }
