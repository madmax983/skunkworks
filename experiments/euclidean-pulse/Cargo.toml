[package]
name = "euclidean-pulse"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
crossbeam-channel = "0.5"
crossterm.workspace = true
parking_lot = "0.12"
rand.workspace = true
ratatui.workspace = true
sysinfo = "0.33"

[dependencies.cpal]
version = "0.15"
optional = true

[features]
default = [] # Audio disabled by default to pass CI in environments without ALSA
audio = ["cpal"]
