[package]
name = "harmony-of-spheres"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
cpal = { version = "0.15", optional = true }
ringbuf = "0.3"
rand = { workspace = true }
anyhow = { workspace = true }

[features]
default = ["audio"]
audio = ["cpal"]
