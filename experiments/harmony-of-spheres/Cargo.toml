[package]
name = "harmony-of-spheres"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = { version = "0.4", default-features = false }
anyhow = "1.0"

[dependencies.cpal]
version = "0.15"
optional = true

[features]
default = []
audio = ["cpal"]
