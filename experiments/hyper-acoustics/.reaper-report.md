# Reaper Forensic Report: `hyper-acoustics`

**Specimen**: `hyper-acoustics`
**Date of Condemnation**: 2026-08-12
**Status**: Condemned (Awaiting Execution)

## Forensic Diagnosis

Specimen `hyper-acoustics` exhibits terminal characteristics and ecosystem maladaptation.

**Terminal Compilation Failure:**
The specimen fails to compile due to structural changes in its parent dependency `hyper-system`. It attempts to access the private modules `math` and `monitor`:
```rust
error[E0603]: module `math` is private
  --> experiments/hyper-acoustics/src/main.rs:46:19
   |
46 | use hyper_system::math::Vec4;
```
Additionally, the specimen relies on math methods (e.g., `rotate_xw`, `rotate_yw`, `rotate_zw`, `project_to_3d`) that do not exist on the imported `Vec4` type from `macroquad::math::Vec4` after the namespace collision/refactor.

**Ecosystem Maladaptation:**
The workspace has evolved, and the `hyper-system` crate was refactored to encapsulate its modules and export them at the root level, pointing to `locus::Vec4`. The specimen `hyper-acoustics` failed to adapt to these genetic shifts and remains broken, causing friction in the repository's build pipeline.

**Conclusion:** CONDEMN.
Grace period: 24 hours.
Salvation requires: Fixing the module access visibility errors and adapting to the new `Vec4` API, or demonstrating significant emergent behavior that warrants its resurrection.