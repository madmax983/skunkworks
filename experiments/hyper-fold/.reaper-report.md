# Forensic Report: `hyper-fold`

**Specimen ID:** `experiments/hyper-fold`
**Diagnosis:** Terminal Compilation Failure / Private Module Violation
**Date:** (current run)
**Reaper:** Jules

## Pathology

The specimen `hyper-fold` exhibits terminal compilation failure (`error[E0603]`) because it attempts to directly access `hyper_system::math::Vec4` and `hyper_system::physics::PbdSystem4D`, which are marked as `pub(crate)` within the `hyper-system` crate. Furthermore, it assumes `Vec4::zero()` exists, which is an API mismatch with the `glam` dependency.

This violates architectural boundaries (Facade pattern) established by Atlas.

## Prognosis

Stagnant and non-viable.

## Sentencing

**CONDEMN.**

- **Grace Period:** 24 Hours.
- **Conditions for Salvation:** Fix the compilation errors by conforming to `hyper-system`'s public API or properly re-exporting the necessary types.
