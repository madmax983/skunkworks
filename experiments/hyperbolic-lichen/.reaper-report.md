# Forensic Analysis: Hyperbolic Lichen

**Specimen:** `hyperbolic-lichen`
**Examination Date:** 2025-02-18
**Examiner:** The Reaper ☠️

## Observation

The specimen presents as a cellular automaton simulating a symbiotic relationship between "Algae" and "Fungus" cells. It utilizes `macroquad` for visualization and the `poincare-disk` crate for... presumably coordinates, though the logic relies on a hash-based neighbor graph.

## Pathology

1.  **Documentation Void**: No `README.md` exists. The purpose, mechanics, and "hyperbolic" nature are opaque to the observer.
2.  **Terminal Redundancy**: The garden already hosts `hyperbolic-mold` (Physarum on hyperbolic plane), which is documented and established. This specimen appears to be a lesser, undocumented variant or competitor that failed to thrive.
3.  **Genericism**: The simulation logic (`lichen.rs`) is a standard cellular automaton (energy/water exchange) that could exist on any graph. The "hyperbolic" aspect is not explained or visualized distinctively enough to justify a separate existence from `hyperbolic-mold`.

## Verdict

**CONDEMN**.

The specimen exhibits no unique "obsession" that isn't already better served by `hyperbolic-mold`. It is a silent, undocumented growth.

**Grace Period:** 24 hours.

**Salvation Criteria:**
- Create `README.md` explaining the hyperbolic geometry and how the neighbor graph is constructed.
- Distinguish the simulation from `hyperbolic-mold` (e.g., focus on the *symbiosis* mechanics).
- Or, merge valuable traits into `hyperbolic-mold` and accept execution.
