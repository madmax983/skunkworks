# Forensic Analysis: lithosphere-termites

**Specimen ID:** `lithosphere-termites`
**Status:** CONDEMNED
**Diagnosis:** Documentation Void / Skeletal Rot (Unused Code) / Terminal Genericism

## Observations

1.  **Documentation Void:** The specimen lacks a `README.md`. There is no instruction on how to interpret the simulation, no description of the "termites" logic, and no explanation of the connection between Git history and geological events. The code is silent.

2.  **Skeletal Rot (Unused Code):**
    -   `experiments/lithosphere-termites/src/terrain.rs`: Fields like `water` are declared but never read. Methods `set_height`, `add_height`, and `remove_height` are defined but never used.
    -   `experiments/lithosphere-termites/src/history.rs`: The `reset` method is dead code.
    -   The existence of these unused organs suggests a transplant from another organism or an abandoned feature set.

3.  **Terminal Genericism:**
    -   The "termite" logic (`src/termites.rs`) is a rudimentary random walker with basic gradient following. It lacks the emergent complexity seen in `term-fluids` or `luminous-flock`.
    -   The connection to Git history is fragile; the simulation falls back to random noise ("demo mode") if it cannot read the repository history, indicating the primary mechanism is not robust.

## Prognosis

The specimen fails to thrive. It mimics the appearance of a complex system (termites, geology, git history) but lacks the internal coherence to sustain life. The "termites" are merely particles following a heightmap, and the "geology" is a simple grid with unused hydrological features.

## Verdict

**CONDEMN.**
**Grace Period:** 24 hours.
**Salvation Requires:**
1.  **Documentation:** A complete `README.md` explaining the simulation's purpose and mechanics.
2.  **Surgical Intervention:** Removal or implementation of unused code (hydrology system).
3.  **Evolution:** Enhancement of termite logic to demonstrate emergent behavior beyond simple gradient ascent.
