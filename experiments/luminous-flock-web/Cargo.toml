[package]
name = "luminous-flock-web"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = { version = "0.8", features = ["small_rng"] }
ratzilla = "0.3"
getrandom = { version = "0.2", features = ["js"] }
