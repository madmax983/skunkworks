[package]
name = "luminous-tank"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
crossbeam-channel = "0.5"
anyhow = { workspace = true }
resonance-audio = { workspace = true }
locus = { path = "../../crates/locus" }
rayon = "1.10"
rand = "0.8"

[dependencies.cpal]
version = "0.15"
optional = true

[features]
default = []
audio = ["dep:cpal"]
