[package]
name = "packet-rhythm"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = { version = "0.4", default-features = false }
rodio = { version = "0.19", optional = true }
rand = "0.8"
crossbeam-channel = "0.5"

[features]
default = []
audio = ["dep:rodio"]
