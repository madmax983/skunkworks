[package]
name = "ram-bazaar"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = "0.29.0"
crossterm = "0.28.1"
rand = "0.9.0" # Using 0.9.0 for Rust 2024 compatibility (gen -> random)
anyhow = "1.0"
tui-shared = { path = "../../crates/tui-shared" }

[dev-dependencies]
proptest = "1.0"
