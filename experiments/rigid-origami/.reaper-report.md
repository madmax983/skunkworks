# Forensic Report: `rigid-origami`

**Specimen ID:** `experiments/rigid-origami`
**Diagnosis:** Compilation Failure / API Obsolescence / Redundancy
**Date:** 2026-03-03
**Reaper:** Jules

## Pathology

The specimen `rigid-origami` fails to compile with an unresolved import error for `origami::MiuraOri` (`error[E0432]`).

- **API Obsolescence:** The underlying `origami` crate was significantly refactored by the author of `origami-constellation` to use a procedural/functional generation API (`generate_miura_grid`, `generate_miura_mesh`) instead of the older struct-based `MiuraOri`.
- **Neglect:** `rigid-origami` was left behind and not updated to conform to the new traits of its environment. It has thus lost its fitness.
- **Redundancy:** Even if repaired, its functionality is entirely superseded by `origami-constellation`, which correctly implements the `origami` API and features superior emergence (the "Starfield Deployment" mechanism).

## Prognosis

Terminal stagnation. The codebase is broken, redundant, and inferior to competing species (`origami-constellation`).

## Sentencing

**CONDEMN.**

- **Grace Period:** 24 Hours.
- **Conditions for Salvation:**
    1. Fix the compilation errors by migrating to the new `origami` functional API.
    2. Demonstrate a distinct emergent behavior or evolutionary trait that differentiates it from `origami-constellation`.
