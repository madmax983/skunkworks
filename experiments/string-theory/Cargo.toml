[package]
name = "string-theory"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
cpal = { version = "0.15", optional = true }
walkdir = "2"
anyhow = "1.0"
rand = "0.8"
crossbeam-channel = "0.5"

[features]
default = []
audio = ["cpal"]
