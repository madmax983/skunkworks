[package]
name = "system-attractor"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4.15"
sysinfo = "0.30"
rayon = "1.10"
anyhow = "1.0"

[dependencies.cpal]
version = "0.15"
optional = true

[dependencies.ringbuf]
version = "0.3"
optional = true

[features]
audio = ["dep:cpal", "dep:ringbuf"]
