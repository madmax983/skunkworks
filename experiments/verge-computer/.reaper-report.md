# ☠️ Forensic Report: verge-computer

**Subject:** `verge-computer`
**Status:** CONDEMNED
**Date:** 2026-04-19
**Judge:** The Reaper ☠️

## 🔍 Evidence of Decay

### 1. Ecosystem Maladaptation
The specimen explicitly isolates itself from the natural swarm environment. By placing itself in the `exclude = ["experiments/verge-computer"]` block of the workspace `Cargo.toml`, it acts as a severed limb, refusing to evolve alongside its sibling organisms.

### 2. Swarm Neglect
Because it has been manually excluded, the standard continuous integration commands such as `cargo test` and `cargo check --workspace` bypass it entirely. This is a tell-tale sign of terminal stagnation, hiding potential errors and code rot.

## ⚖️ Judgment

**CONDEMN.**
Any organism that actively severs itself from the repository's lifecycle is dead code walking. A true chimera thrives within the ecosystem; this specimen hides from it.

## ⏳ Grace Period
**24 Hours.**
To survive, the specimen must:
1. Be removed from the `exclude` list in the workspace `Cargo.toml`.
2. Compile and run cleanly as a fully integrated member of the ecosystem, resolving any dependency or borrowing failures that justified its initial quarantine.
