# ☠️ Forensic Report: verge-computer

**Subject:** `verge-computer`
**Status:** CONDEMNED
**Date:** 2026-04-20
**Judge:** The Reaper ☠️

## 🔍 Evidence of Decay

### 1. Ecosystem Maladaptation
The experiment explicitly isolates itself from the workspace compilation and testing infrastructure. It resides within the `exclude = ["experiments/verge-computer", ...]` block in the workspace `Cargo.toml`. When the swarm attempts to run workspace-wide commands, this specimen hides. Furthermore, running local checks such as `cargo check -p verge-computer` fails due to rustc version resolution conflicts on sub-dependencies (e.g. `constant_time_eq`).

### 2. Swarm Neglect
By severing itself from the workspace and remaining uncompilable within the standard local environment, the specimen fails to prove its right to exist in the current ecosystem.

## ⚖️ Judgment

**CONDEMN.**
The specimen exhibits Ecosystem Maladaptation by separating itself from the workspace CI/CD pipeline and failing to compile due to rustc version incompatibilities.

## ⏳ Grace Period
**24 Hours.**
To survive, the specimen must:
1. Reintegrate into the ecosystem by compiling cleanly alongside the workspace (e.g., removing itself from the `exclude` list and fixing its rustc version requirements).
