[package]
name = "code-concerto"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
rodio = { version = "0.19", optional = true }
syn = { version = "2.0", features = ["full", "extra-traits", "visit"] }
quote = "1.0"
anyhow = "1.0"
rand = "0.8"
walkdir = "2.4"

[features]
default = []
audio = ["dep:rodio"]
