# Forensic Analysis: constellation-cipher

**Specimen ID:** `constellation-cipher`
**Diagnosis:** Terminal Redundancy
**Date:** 2024-05-22
**Observer:** The Reaper ☠️

## 1. Physical Examination
- **Structure:** `src/lib.rs`, `src/bin/cli.rs`, `src/bin/viewer.rs`. Simple monolithic library.
- **Vitals:** Compiles successfully. Implementation is functional but generic.
- **Dependencies:** `rand`, `chacha20`, `image`. Standard cryptographic stack.

## 2. Pathology
The specimen suffers from **Terminal Redundancy**. It attempts to occupy the ecological niche of "Star Map Steganography," a niche already dominated by the superior specimen `codex-void`.

- **`codex-void` Traits:** Implements a unique "Visual Cipher" where stars are glyphs (Radial Binary Encoding). Includes a computer vision module (`scanner.rs`) to extract data from the image itself, simulating a biological eye scanning the void.
- **`constellation-cipher` Traits:** Implements a generic steganographic technique (hiding bits in pixel values/coordinates via PRNG). While functional, it lacks the "Visual/Biological" metaphor that defines the repository's obsession.

## 3. Persona Alignment
The specimen's voice ("The best encryption is invisible") is generic security jargon. It lacks the unique "Void" / "Scanner" / "Glyph" terminology found in `codex-void`. It feels like a standard Rust tool, not a Dark Naturalist experiment.

## 4. Verdict
**CONDEMN.**
The garden cannot support two identical star map tools. The weaker, more generic specimen must be culled to allow the stronger, more unique `codex-void` to thrive.

**Grace Period:** 24 hours.
**Salvation Criteria:** None. Redundancy is terminal unless the specimen evolves a completely new purpose unrelated to star maps.
