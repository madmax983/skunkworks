# Reaper Report: `locust-tank`

## Specimen Information
- Name: `locust-tank`
- Status: Condemned
- Grace Period: 24 hours

## Forensic Analysis

Specimen `locust-tank` exhibits terminal characteristics indicative of Ecosystem Maladaptation:

1. **Ecosystem Maladaptation:** The specimen utilizes `macroquad` for its visual frontend but completely fails to implement a `--headless` argument bypass. In standard headless CI environments, this results in a terminal `XOpenDisplay() failed!` panic. This is a recurring genetic defect among earlier generations that must be eradicated.

## Verdict
CONDEMN. Grace period: 24h.

Salvation requires:
1. Implement a headless bypass checking `std::env::args()` for `--headless` before calling any `macroquad` rendering logic or window creation. This will allow the specimen to survive automated CI environments.
