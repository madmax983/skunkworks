# Forensic Report: penrose-build

## Specimen Identity
- **Name:** penrose-build
- **Location:** experiments/penrose-build
- **Status:** Condemned

## Terminal Characteristics
1.  **Documentation Void:** Specimen lacks `README.md`. Purpose is inferred from code inspection (`TUI` dependency visualizer).
2.  **Ancestral Redundancy:** Specimen appears redundant with `penrose-deps`, which utilizes `macroquad` for isometric visualization and implements more robust dependency resolution via `metadata.resolve`.
3.  **Sketch Quality:**
    - Graph construction logic in `graph.rs` relies on manual parsing/heuristics rather than the standard `resolve` graph provided by `cargo_metadata`.
    - `Cargo.toml` specifies `edition = "2024"`, which is anomalous/premature.
    - Implementation seems to be a rough sketch of a TUI isometric renderer.

## Necropsy Findings
- **Vital Signs:** Compiles (workspace member).
- **Entropy Level:** Moderate.
- **Scent:** The smell of fresh paint on a structure that is already leaning.

## Verdict
**CONDEMN.**
Grace period: 24 hours.
Salvation requires:
- **Differentiation:** Clearly define unique value proposition vs `penrose-deps` (e.g., specific TUI interaction model).
- **Documentation:** Complete `README.md`.
- **Stabilization:** Fix edition, use proper dependency resolution logic.
