# Forensic Report: turing-terra

## Specimen Identity
- **Name:** turing-terra
- **Path:** experiments/turing-terra
- **Detected Persona:** Generic (Turing Pattern / Reaction-Diffusion)

## Diagnosis: Terminal Redundancy

This specimen exhibits code sequences nearly identical to `reaction-monitor`, but lacks the evolutionary adaptation of environmental sensing.

### Evidence
1.  **Code Duplication:** The `State` struct, `Uniforms` struct, and shader pipeline are clones of `reaction-monitor`.
2.  **Devolution:**
    - `reaction-monitor`: Uses `sysinfo` to map CPU/RAM usage to `feed` and `kill` parameters. It "lives" off the host machine's stress.
    - `turing-terra`: Uses manual arrow key input. It requires a puppeteer.
3.  **Persona Failure:** The specimen fails to demonstrate unique "Genesis" or "Nova" traits. It is a standard `wgpu` tutorial implementation of Gray-Scott reaction-diffusion.

## Judgment
**CONDEMNED.**
The garden has no room for manual toys when autonomous organisms exist. `reaction-monitor` is the superior species.

## Sentence
Execution scheduled in 24 hours unless:
1.  It develops a unique sensory organ distinct from `reaction-monitor`.
2.  It implements a novel visualization technique beyond standard 2D texture mapping.
