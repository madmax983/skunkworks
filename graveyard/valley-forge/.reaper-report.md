# Forensic Report: `valley-forge`

**Date:** 2026-02-14
**Reaper:** ☠️ The Reaper

## Specimen Identification
- **Name:** `valley-forge`
- **Location:** `experiments/valley-forge`
- **Lineage:** Unknown (Generic Terrain/Advection)

## Terminal Characteristics

1.  **Terminal Compilation Failure:**
    Forensic analysis of `src/main.rs` reveals a fatal structural defect. The code initializes `Vertex` with a `normal` field:
    ```rust
    normal: Vec4::ZERO, // Fixed: was Vec3::ZERO
    ```
    However, in the local environment (`macroquad = "0.4"`), the standard `Vertex` struct does not possess this field. This indicates the specimen was either copy-pasted from an incompatible source or hallucinated features it does not possess.

2.  **Documentation Void:**
    No `README.md` exists. The specimen provides no instructions or context.

3.  **Redundancy:**
    The specimen implements a generic reaction-diffusion-advection heightmap visualization. This niche is already occupied by hybrids like `fluid-specter` and `luminous-valley`. The latter is explicitly noted as a "Hybrid of `valley-forge` and `luminous-flock`", suggesting `valley-forge` has already passed its genes to a superior successor.

## Judgment
**CONDEMN.**

The specimen is broken (compilation failure) and redundant (traits preserved in offspring). It has served its evolutionary purpose and is now dead weight.

## Grace Period
24 Hours.

## Salvation Criteria
1.  **Fix Compilation:** Remove the invalid `normal` field from `Vertex` initialization.
2.  **Document:** Create a `README.md`.
3.  **Differentiate:** Prove it offers value distinct from `luminous-valley`.
