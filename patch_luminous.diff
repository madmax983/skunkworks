--- experiments/luminous-flock-web/src/world.rs
+++ experiments/luminous-flock-web/src/world.rs
@@ -27,7 +27,7 @@
         let mut phase_nudges = vec![0.0; count];

-        for i in 0..count {
+        for (i, nudge) in phase_nudges.iter_mut().enumerate().take(count) {
             let boid = &self.boids[i];
             let mut sep = Vec2::ZERO;
             let mut ali = Vec2::ZERO;
@@ -57,7 +57,7 @@
                 }
             }

-            phase_nudges[i] = total_phase_nudge;
+            *nudge = total_phase_nudge;

             if neighbors > 0 {
