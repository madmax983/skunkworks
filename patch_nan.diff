--- experiments/chimera-lang/src/vm/nova_ballistics.rs
+++ experiments/chimera-lang/src/vm/nova_ballistics.rs
@@ -31,6 +31,10 @@
             continue;
         }

+        if p.x.is_nan() || p.y.is_nan() {
+            continue;
+        }
+
         // Check bounds
         if p.x < 0.0 || p.x >= GRID_SIZE as f64 || p.y < 0.0 || p.y >= GRID_SIZE as f64 {
             continue;
