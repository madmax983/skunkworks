1. **Fix Architectural Tangle in `literary-boids` (and graveyard crates):** The crate `alleles/literary-boids` (along with several graveyard crates) failed to compile due to a structural leak: it was referencing `tui_shared::math::Vec2`, a module that was extracted out to `locus::Vec2` in a prior architectural refactoring. I have redirected the imports to `locus::Vec2` and updated `alleles/literary-boids/Cargo.toml` to depend on `locus`.
2. **Enforce Boundary Constraints (Facade Pattern) in `literary-boids`:** `alleles/literary-boids/src/main.rs` leaked internal submodules directly via `pub mod` (`boid`, `critic`, `syntax_physics`, `traces`, `world`). I changed these to `pub(crate) mod` to encapsulate the domain models and strictly adhere to Atlas's Facade pattern rule.
3. **Refactor Redundant DNA Definition:** In `alleles/literary-boids`, `DNA` was a capitalized acronym violating Rust conventions (and triggering Clippy warnings). I renamed it to `Dna` across `boid.rs`, `world.rs`, and `syntax_physics.rs`.
4. **Remove Dead Code:** Removed unused distance functions (`distance`, `distance_squared`, `limit`) in `boid.rs` and replaced them with direct method calls on `locus::Vec2` inside `world.rs` to flatten unnecessary abstractions.
5. **Update Atlas Journal:** Append an entry to `.jules/atlas.md` documenting the structural decoupling of `Vec2` from `tui-shared` to `locus` in the `literary-boids` experiment, and the enforcement of the Facade pattern.
6. **Pre Commit Steps:** Complete pre-commit steps to make sure proper testing, verifications, reviews and reflections are done.
7. **Submit:** Submit a PR titled "🗺️ Atlas: Decouple vector math and enforce Facade in literary-boids" capturing the Tangle, Blueprint, Stability, and Verification.
