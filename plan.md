1. Verify `arthropod-poincare` compiles.
   - Run `cargo check -p arthropod-poincare` to ensure the new experiment has no compilation errors.
2. Complete pre commit steps
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
3. Submit the git changes.
   - Use the `submit` tool with the format `🧬 Splice: Cross arthropod × poincare-disk` as the commit message.
