1. Modify `crates/physics-pbd/tests/havoc.rs` to constrain `dt` to a positive range (e.g., `0.001f32..0.1f32`) so the step function executes the physics simulation instead of returning early on invalid `dt`. Also, add a `stiffness` parameter to the proptest (using `proptest::num::f32::ANY`), and pass it to `system.add_distance_constraint` to trigger the constraint panic deterministically.
2. Modify `crates/hyper-system/tests/havoc.rs` similarly. Constrain `dt` to `0.001f32..0.1f32`, set `iterations` to start from `1` (so constraints are processed), and replace the constant `1.0` stiffness with a fuzzed `stiffness` value from `proptest::num::f32::ANY`. Ensure `friction` remains a valid value (e.g., `0.98`) if its NaN-ness would cause early exits or unexpected behavior, although fuzzing it is also acceptable as long as the panic triggers.
3. Run `cargo test -p physics-pbd --test havoc` and `cargo test -p hyper-system --test havoc` to verify that both tests consistently panic as expected (`#[should_panic]` tests pass).
4. Run pre commit checks.
5. Create a PR formatting it as '👹 Havoc: [Explosion Target]' detailing The Trigger, Stack Trace, Reproduction, and a chaotic Comment.
