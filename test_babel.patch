--- experiments/chimera-lang/src/vm/babel.rs
+++ experiments/chimera-lang/src/vm/babel.rs
@@ -743,18 +743,14 @@
                     }
                 }
                 "Seq" => {
-                    let rules = args
-                        .iter()
-                        .skip(1)
-                        .map(value_to_grammar_rule)
-                        .collect::<Vec<_>>();
+                    let mut rules = Vec::with_capacity(args.len().saturating_sub(1));
+                    rules.extend(args.iter().skip(1).map(value_to_grammar_rule));
                     return GrammarRule::Sequence(rules);
                 }
                 "Alt" => {
-                    let rules = args
-                        .iter()
-                        .skip(1)
-                        .map(value_to_grammar_rule)
-                        .collect::<Vec<_>>();
+                    let mut rules = Vec::with_capacity(args.len().saturating_sub(1));
+                    rules.extend(args.iter().skip(1).map(value_to_grammar_rule));
                     return GrammarRule::Choice(rules);
                 }
                 "Ref" => {
