# doom-rs keeper PR merge log

**Repo:** madmax983/doom-rs
**Baseline (origin/trunk @ 06c9178):**
- Build: OK after installing `libasound2-dev` (alsa) — required by `alsa-sys` transitively via doom-audio's cpal.
- Clippy `-D warnings`: **PRE-EXISTING FAILURE** in doom-tui (deprecated `set_skip` in `event_loop.rs:167` and `sixel.rs:117`). This was later fixed as a bonus in #1223.
- Tests: all pass (workspace, all-features).

**Keepers processed (ascending):** 645, 691, 730, 785, 993, 1065, 1223, 1270
**Dups left open (not touched):** 587 (dup of 691), 724 (dup of 730)

**Final trunk head after all merges:** `4224ad9` — full-workspace `cargo clippy --workspace --all-targets --all-features -- -D warnings` PASSES; `cargo test --workspace --all-features` passes (28 `test result: ok` lines, zero failures).

| PR# | outcome | notes |
|-----|---------|-------|
| 645 | **MERGED** (squash `2e0b03f`) | Rebased clean. Dropped junk `pr_description.md` hunk + reverted unrelated `lib.rs` fmt. Kept 3 source `unreachable!()`→fallback changes (`glyphs.rs`, `phase.rs`, `telemetry.rs`) + `.jules/sentry.md` + new tests. All three sites are defensively-unreachable on current trunk (masked / outer-guarded), but harmless panic-hardening. `doom-game` clippy clean; `doom-app`'s only clippy errors were pre-existing doom-tui `set_skip`. Tests pass. No CI configured. |
| 691 | **MERGED** (squash `0f5ee7b`) | Trig-table data-race fix: `static mut SINE_TABLE` + `AtomicBool` → `once_cell::sync::OnceCell`; `init_trig_tables` becomes safe fn; cascading `unsafe {}` removals across 12 files. Genuine soundness fix (the keeper per dedup note). Also deletes junk `pr_description.md`, adds `once_cell` dep. Caveat: bundled `from_name` `split_once('M')` change does NOT fix a real panic — empirically `'M'` is ASCII so byte-indexing never lands mid-char (`E❤M1` returns `None` cleanly on trunk); it's a behavior-preserving cleaner refactor, harmless. `doom-types` + `doom-game` clippy clean; all affected-crate tests pass. Dup #587 left open. Minor cosmetic nit: an `angle.rs` rustfmt line-wrap fixup wasn't committed before push (no fmt CI, clippy + tests green — non-blocking). Reverted unrelated `lib.rs` fmt. No CI. |
| 730 | **MERGED** (squash `45a88d0`) | Genuine `i32` overflow fix in `p_radius_attack`: `damage * (radius_int - dist_i)` overflows for large damage (e.g. `i32::MAX`) → cast operands to `i64` for the multiply + clamp back to `i32`. `combat.rs` + `.jules/havoc.md` log. New test `havoc_test_radius_attack_overflow_fix` passes; all 1364 `doom-game` tests green; clippy clean. Reverted unrelated `lib.rs` + `angle.rs` fmt. Dup #724 left open. No CI. |
| 785 | **MERGED** (squash `2556052`) | Test-coverage PR: +10 unit tests for `savegame_vanilla` (`parse_header` / `version_string` / `load_game` / `save_game`) boundary + error paths (all API symbols verified against trunk, all 10 pass). Also removes junk root `fix.py`. Clippy clean. Not a source bugfix but low-risk coverage + junk cleanup — kept per instructions. Reverted unrelated `lib.rs` / `angle.rs` fmt. No CI. |
| 993 | **MERGED** (squash `5bd089d`) | Genuine `i32` overflow-panic fix in `BitmapFont::draw_string`: `x + (i as i32) * i32::from(self.char_width)` overflows for huge strings → `saturating_add`/`saturating_mul` + off-screen (`cx >= FB_WIDTH`) early break; `draw_string_centered` uses `saturating_sub`. 1 file. 2 new tests pass; `doom-renderer` clippy clean. Note: the centered-overflow test takes ~65 s (iterates ~150M off-screen chars before the early-break trips on a pathological 300M-char input) — bounded, no panic/OOM, normal input unaffected. No unrelated fmt. No CI. |
| 1065 | **MERGED** (squash `79be720`) | Genuine infinite-loop / OOM fix: `render_mus_to_wav_mono` spins forever pushing samples on an empty / 0-duration MUS score (`completed_loops` only increments on score wrap-around, which never happens without events) → early return an empty WAV. `wav.rs` + `.jules/havoc.md` + new timeout-guarded test. Removed junk `commit_message.txt`. Resolved `havoc.md` conflict (kept trunk's post-#730 entry + appended MUS entry). `doom-audio` clippy + full suite clean; test passes instantly. No CI. |
| 1223 | **MERGED** (squash `9553a83`) | Big keeper covering both stated audio bugs **and** fixing the pre-existing clippy baseline: (1) `MidiPlayer::advance_samples` — `saturating_add` for `sample_count`/`next_event_tick`, `sample_rate.max(1)` divide-by-zero guard, clamp `n_samples` to 88200 (2s audio) to prevent giant `Vec` OOM, bounds-check `idx`/`drop` against `src.len()`; (2) `wav::encode_pcm16_wav_mono` — `saturating_mul`/`saturating_add` for `byte_rate`/`data_size`/`riff_size`; (3) **BONUS**: wraps `doom-tui` `set_skip` in `#[allow(deprecated)]` at `event_loop.rs:167` AND `sixel.rs:117`, which **fixes the pre-existing baseline clippy `-D warnings` failure**. First time in this session that `cargo clippy --workspace --all-targets --all-features -- -D warnings` cleanly passes. 2 new havoc tests pass, `doom-audio` + `doom-tui` suites clean. Resolved `havoc.md` conflict; kept `pr_description.md` deleted (was already deleted on trunk by #691). No CI. |
| 1270 | **MERGED** (squash `4224ad9`) | Genuine silent-data-corruption fix in `Blockmap::block_linedefs`: OOB `(col, row)` with `self.offsets.get(idx).copied().unwrap_or(0) as usize` was silently reading the file header as bogus linedef indices; now returns empty by starting `pos` at `data.len()` for the `None` case. 1 source file (`crates/doom-map/src/lumps.rs`) + `.jules/havoc.md` log entry. Resolved `havoc.md` conflict. `doom-map` tests + full-workspace clippy `-D warnings` clean. No CI. |

## Summary

- **8/8 keeper PRs merged**, all via squash, in ascending order (645, 691, 730, 785, 993, 1065, 1223, 1270).
- **0 skips**, 0 close-on-closer-look, 0 blocks (one 529 API pause on the #1223 push, retried successfully).
- **Duplicates left open (untouched):** #587, #724.
- **Trunk never force-pushed.** Only feature branches were force-with-lease-pushed after rebase.
- **Post-merge state:** trunk head `4224ad9`, workspace builds, `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes cleanly, `cargo test --workspace --all-features` passes with zero failures.
- **Junk / cruft removed via merges:** `fix.py` (#785), `commit_message.txt` (#1065), `pr_description.md` remains deleted (originally removed via #691's rebase). No `.orig` / `.rej` files were found in any keeper.
- **Recurring cleanup pattern:** each PR touched `.jules/havoc.md` (or `.jules/sentry.md`); several PRs conflicted on that log file during rebase — resolved by preserving trunk's cumulative entries + appending the PR's own new entry. Every PR also generated a small unrelated fmt diff on `crates/doom-app/src/lib.rs` (an empty file) and (after #691) on `crates/doom-types/src/angle.rs` (a minor line-wrap nit that was merged uncleaned in #691); both were reverted from every subsequent PR's branch to keep diffs focused.
