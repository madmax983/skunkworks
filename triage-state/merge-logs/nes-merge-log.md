# NES Keeper-PR Merge Log

| PR# | outcome | notes |
|-----|---------|-------|
| 788 | closed-on-closer-look | Wrong-premise/ineffective: `to_js_error` uses `JsValue::from_str` (wasm32-only), so the mutant it targeted is genuinely unkillable by the native `cargo test`/`cargo-mutants` that flagged it — the PR body concedes this. The added test is `#[cfg(target_arch = "wasm32")]`-gated so it never runs in this repo's CI (no `wasm-pack` step), it leaves `use super::*;` unused on native — which fails `cargo clippy --workspace --all-targets --all-features -- -D warnings` (verified locally), and it pins `wasm-bindgen-test = "0.2"` (the correct pair for wasm-bindgen 0.2.x is `"0.3"`), bloating `Cargo.lock` with ancient duplicate crates (`futures 0.1`, `proc-macro2 0.4`, `quote 0.6`, `cfg-if 0.1`, `unicode-xid 0.1`). Net-negative with no verifiable coverage gain. |
| 827 | merged (squash `78a99e4`) | Genuine security fix (bound `BufRead::read_line` on untrusted MCP/relay/netplay streams to prevent OOM DoS). Salvaged: removed junk `test_oom.rs`, restored wrongly-deleted `crates/nes-dsl/tests/test_coverage_gap2.rs`, restored `plan.md` from trunk (unrelated scratch overwrite), ran `cargo fmt` to fix de-indented `fn` after `#[test]` and stray leading blank lines. Kept the legit junk deletions (`crates/nes-mcp/src/output.rs.orig`, `test_coverage.patch`). Verified locally: `cargo fmt --check` clean, `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean, `cargo test --workspace --all-targets --all-features` `CARGO_EXIT=0` (118 test binaries, 0 fails, all salvaged tests including `test_resolve_addressing_mode`, the bounded-read tests, and the anti-infinite-loop test run green). CI was still queued at merge time (runner backlog); trunk CI green; merge API accepted → no required check was blocking. |
| 891 | merged (squash `9b13757`) | Rebase resolved three conflicts (`sentry.md` — kept both entries; `havoc_mcp_oom.rs` + `mcp_host_slowloris.rs` — trunk versions from #827 already have identical `#![cfg(feature = "mcp-host")]` gates). Final diff: `.jules/sentry.md` +4, `crates/nes-core/src/api.rs` +71 (new `tests_api_coverage_gaps` module), `mcp_host_slowloris.rs` +1/-1 (comment tweak). Local: fmt clean, clippy clean, `cargo test --workspace --all-targets --all-features` `CARGO_EXIT=0`, both new tests (`test_core_snapshot_mapper_delta_coverage`, `test_execute_unsupported_commands_returns_ok`) pass. |
| 905 | merged (squash `6e6e4c9`) | Clean rebase, no conflicts. Docs + `nes-core/src/cheat_codes.rs` `mutant_hunting` module (+56 lines) exercising `raw()`, `address()`, `value()`, `compare()`, `applies_to()` accessors. Local: fmt/clippy clean, all 5 new tests pass (`cargo test -p nes-core --lib mutant_hunting`). |
| 913 | merged (squash `63917c1`) | Rebased over already-merged 891 with one conflict in `.jules/sentry.md` (kept all three prior entries + new one). Adds error-boundary tests: `test_deserialize_too_long` in `serde_array.rs`, `encode_ppm_returns_error_on_buffer_too_large` in `ppm.rs`. Local: fmt/clippy clean, all 8 relevant tests pass. |
| 935 | closed-on-closer-look | Wrong-premise: `crates/nes-dsl/src/assembler.rs` is dead orphan code — `crates/nes-dsl/src/lib.rs` never declares `mod assembler;` (only `mod opcode;`), so cargo doesn't compile that file at all. The live `Assembler::emit_expr_byte` inlined in `lib.rs` line 755 already returns `Err(DslError::Parse { message: "internal error: byte emitter used for word fixup" })` for `(_, FixupKind::Word)` — the exact same fix this PR claims to add. Also, the added test (`test_emit_expr_byte_returns_error_for_word_fixup` inside `assembler.rs`) is never compiled/executed. Any real cleanup would be a separate PR that deletes the four orphan files (`assembler.rs`, `ast.rs`, `parser.rs`, `rom.rs`), not one that pretends they're live. |
| 953 | merged (squash `af1b9b3`) | Clean rebase, no conflicts. Docs-only: adds a copy-paste `cp config/ai/profiles/smb-control.example.toml config/ai/profiles/smb-control.toml` block to the top-level README and to `crates/nes-ai/README.md`. |
| 960 | closed-on-closer-look | Superseded/empty: the branch has zero changed files vs `trunk`, and the intended fix (adding `#![cfg(feature = "mcp-host")]` to `crates/nes-desktop/tests/mcp_host_slowloris.rs` and `crates/nes-desktop/tests/havoc_mcp_oom.rs`) was already landed by #827. |
| 962 | merged (squash `fa041be`) | Rebased with one conflict in `.jules/sentinel.md` (kept both entries). Adds `bus_map.rs` explicit boundary assertions, `bus_map_prop.rs` full-`u16` range match, and gxrom padding-math tests. Local: fmt/clippy clean, all 3 new test targets pass. |
| 972 | merged (squash `abb78c9`) | Salvaged: original PR only added a `docs/ECHO_README_REPORT.md` scratch file describing the fix but never touched `README.md`. Deleted the scratch file and applied the actual DX fix — a `rustup target add wasm32-unknown-unknown` line before the wasm `cargo build` in both the top-level `README.md` and `crates/nes-web/README.md`. Docs-only, no fmt/clippy/test needed. |

## Summary

- **Merged:** 7 PRs (827, 891, 905, 913, 953, 962, 972)
- **Closed on closer look:** 3 PRs (788, 935, 960)
- **Blocked:** 0 PRs

Final trunk tip: `abb78c9` (from `ce26964` at the start of the session).
